# Archived reference docs

Moved here during the PostgreSQL / Agila cut-over (2026-08).

These describe the legacy SQLite multi-tenant stack (`ezkan.cloud`, per-tenant
SQLite DBs on NFS, older deploy checklists). Prefer live PG docs and scripts
under `k8s/*-pg.*` and `docs/` for current operations.
